apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ferferga-ckan.fullname" . }}-test-connection"
  labels:
    {{- include "ferferga-ckan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ferferga-ckan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
