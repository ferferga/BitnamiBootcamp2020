kind: PersistentVolume
apiVersion: v1
metadata:
  name: standard-storage
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/minikube"