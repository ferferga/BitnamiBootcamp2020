apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
    tier: frontend
spec:
  ports:
    - port: 80
      targetPort: http-server
  selector:
    app: wordpress
    tier: frontend
  type: NodePort